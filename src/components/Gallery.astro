<div class="gallery">
  {
    [...Array(10)].map((_, index) => (
      <div class="card">
        <h2>Card {index + 1}</h2>
      </div>
    ))
  }
</div>

<style>
  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    /* gap: 1em; */
    width: 100%;
    /* height: 100%; */
    
  }

  .card {
    width: 100%;
    aspect-ratio: 1/1;
    outline: 1px solid #859195;
  }

</style>